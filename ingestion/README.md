# Ingestion — Central Alert Intake

## Purpose

The ingestion layer provides a common entry point for security alerts generated by multiple detection sources.

For this project, the detection sources are:

- Amazon GuardDuty
- AWS CloudTrail-based EventBridge detection

Both sources are routed to the same central SNS topic:

```text
central-security-alerts
